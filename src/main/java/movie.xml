<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="moviens">
	<!-- resultType은 조회할 때만 사용 -->
	<select id="genre" parameterType="stars" resultType="string">
		select genre from movie m, stars s where m.movieNo = s.movieNo
	</select>
	<select id="recommendList" parameterType="string" resultType="movie">
		select * from movie where movie.genre = #{recommendGenre}
	</select>
	<select id="moviePageList" parameterType="hashmap" resultType="movie">
		select * from (select rowNum rn, e.* from 
		(select * from stars s, movie m where s.movieNo = m.movieNo and m.movieGenre = #{genreNo}
		and m.del='n' and s.Score = 0 order by m.movieNo desc) e)
		where rn between #{startRow} and #{endRow}
	</select>
</mapper>